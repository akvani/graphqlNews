const app = require('../app');
const {serverConfig} = require('../config').appConfig;


app.listen(serverConfig.port,() => {
    console.log(`News DB MicroService is running at port ${serverConfig.port}`);
})
